# Separated Portfolio Files

Your original single HTML portfolio has been split into separate files.

Open `index.html` in VS Code or with Live Server.

Main folders:
- `css/` contains separated CSS sections and patches.
- `js/` contains separated JavaScript features such as Firebase, chatbot, certificates, services, and 3D memory palace.

Important: keep the same folder structure so the file links continue working.
